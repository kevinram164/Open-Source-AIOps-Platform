# Grafana Integration

Phase 5: dashboards cho incidents, RCA, automation metrics.

Planned datasources:
- Prometheus (OpenShift monitoring)
- PostgreSQL (incident/RCA history)

PVC: `grafana-data` trong `aiops-observability` (đã tạo ở bootstrap).
