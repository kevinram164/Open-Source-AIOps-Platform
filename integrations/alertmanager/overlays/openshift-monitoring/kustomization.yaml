apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: openshift-monitoring
resources:
  - cluster-monitoring-config.yaml
  - ../base/alertmanagerconfig.yaml
# KHÔNG dùng nameSuffix — ConfigMap phải đúng tên cluster-monitoring-config
