apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespaces/
  - rbac/
  - network-policies/
  - storage/
  - configmaps/

# Secrets: bootstrap/secrets/README.md (manual)
# Ollama: managed by Argo Application aiops-ollama → bootstrap/ollama
# (do not also list ollama/ here — avoids double-sync with aiops-bootstrap)
