apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespaces/
  - rbac/
  - network-policies/
  - storage/
  - configmaps/

# Secrets are applied manually — never commit real values.
# See bootstrap/secrets/README.md
