apiVersion: v1
kind: Service
metadata:
  name: {{ include "remediation-controller.fullname" . }}
  labels:
    {{- include "remediation-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "remediation-controller.selectorLabels" . | nindent 4 }}
