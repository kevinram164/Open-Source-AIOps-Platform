apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.fullname" . }}-datasources
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: https://thanos-querier.openshift-monitoring.svc:9091
        isDefault: false
        jsonData:
          tlsSkipVerify: true
          timeInterval: 30s
          httpHeaderName1: Authorization
        secureJsonData:
          httpHeaderValue1: Bearer $__env{THANOS_BEARER_TOKEN}
      - name: AIOps Postgres
        uid: aiops-pg
        type: postgres
        access: proxy
        url: {{ .Values.postgres.host }}:{{ .Values.postgres.port }}
        user: aiops
        isDefault: true
        editable: true
        secureJsonData:
          password: $__env{GF_POSTGRES_PASSWORD}
        jsonData:
          database: {{ .Values.postgres.database }}
          sslmode: disable
          postgresVersion: 1500
          maxOpenConns: 5
          maxIdleConns: 2
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.fullname" . }}-dashboards-provider
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
data:
  dashboards.yaml: |
    apiVersion: 1
    providers:
      - name: aiops
        orgId: 1
        folder: AIOps
        type: file
        disableDeletion: false
        updateIntervalSeconds: 30
        allowUiUpdates: true
        options:
          path: /etc/grafana/dashboards/aiops
      - name: npd
        orgId: 1
        folder: NPD
        type: file
        disableDeletion: false
        updateIntervalSeconds: 30
        allowUiUpdates: true
        options:
          path: /etc/grafana/dashboards/npd
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.fullname" . }}-dashboard-aiops
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
data:
  aiops-overview.json: |
{{ .Files.Get "dashboards/aiops-overview.json" | indent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.fullname" . }}-dashboard-npd
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
data:
  npd-banking.json: |
{{ .Files.Get "dashboards/npd-banking.json" | indent 4 }}
  npd-shop.json: |
{{ .Files.Get "dashboards/npd-shop.json" | indent 4 }}
  npd-kong.json: |
{{ .Files.Get "dashboards/npd-kong.json" | indent 4 }}
  npd-rabbitmq.json: |
{{ .Files.Get "dashboards/npd-rabbitmq.json" | indent 4 }}
  npd-infra.json: |
{{ .Files.Get "dashboards/npd-infra.json" | indent 4 }}
  npd-ocp-nodes.json: |
{{ .Files.Get "dashboards/npd-ocp-nodes.json" | indent 4 }}
