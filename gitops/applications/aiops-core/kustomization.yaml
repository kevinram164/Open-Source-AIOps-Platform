# aiops-core Argo Applications — reuse shared postgres-ha (ns postgres), no local Postgres chart.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - vault-secrets.yaml
  - db-init.yaml
  - incident-api.yaml
  - rca-agent.yaml
  - aiops-console.yaml
