# Merge label Ambient vào ns đã có (bootstrap). Không enroll aiops-automation.
apiVersion: v1
kind: Namespace
metadata:
  name: aiops-core
  labels:
    istio-discovery: enabled
    # Không Ambient cả ns (Route Grafana/webhook). Bật từng deploy khi enroll.
    app.kubernetes.io/part-of: open-aiops-platform
